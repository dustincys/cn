---
layout: post
category: technology
title: 日程管理的逻辑
matheq: no
comments: yes
tags: [emacs, 日程管理]
share: yes
toc: no
---

> 这个现实世界太混乱、太复杂。

诸多因素导致我的待办事务、邮件、日期提醒、事务预约等错综复杂、相互干扰。
而如何快速合理地整合这些纷纷扰扰，怎可能难住一个电脑专业人士！

日程管理的核心逻辑是，碰到尘世间纷纷扰扰，把其扔进日程管理系统。
然后，让系统每天自动生成待办事项的报告。
就好比是一个不吃不喝的秘书。
这个日程管理系统的必要功能是将各种待办事项进行排序，
且能够查看当前要办理哪些事务，然后查看近期还有哪些事需要办，再然
后查看远期还有哪些事务需要办。
这样就实现了对短程、中程、长程任务的全场景、全任务、无死角地覆盖。

先设置默认的中程场景任务排序：
```elisp
(setq org-agenda-todo-ignore-scheduled 'all)
(setq org-agenda-todo-ignore-deadlines 'far)
(setq org-log-done 'time)
(setq org-agenda-start-with-log-mode t)
```

再设置长程任务排序：
```elisp
(setq org-agenda-custom-commands
            '(
              ("f" "Far plans"
               (
                (todo ""
                      (
                       (org-agenda-skip-function 'my-org/skip-repeated-todos)
                       (org-agenda-todo-ignore-scheduled 'all)
                       (org-agenda-todo-ignore-deadlines 'near)
                       (org-agenda-overriding-header "Far plans")))
                ))
              ("n" "No deadline plans"
               (
                (todo ""
                      (
                       (org-agenda-skip-function 'my-org/skip-repeated-todos)
                       (org-agenda-todo-ignore-with-date t)
                       (org-agenda-overriding-header "No deadline plans")))
                ))
              ("d" "Weekly review"
               (
                (agenda ""
                        (
                         (org-agenda-overriding-header "Completed tasks")
                         (org-agenda-span 'week)
                         (org-agenda-start-day "-6d")
                         (org-agenda-end-day "tomorrow")
                         (org-agenda-skip-function '(org-agenda-skip-entry-if 'nottodo 'done))
                         ))
                ))
              ("rf" "Far repeated plans"
               (
                (todo ""
                      (
                       (org-agenda-todo-ignore-deadlines 'near)
                       (org-agenda-skip-function 'my-org/skip-non-repeated-todos)
                       (org-agenda-overriding-header "Far repeated plans")))
                ))
              ("rt" "Near repeated plans"
               (
                (todo ""
                      (
                       (org-agenda-todo-ignore-deadlines 'far)
                       (org-agenda-skip-function 'my-org/skip-non-repeated-todos)
                       (org-agenda-overriding-header "Near repeated plans")))
                ))
              ))
```

设置重复待办事项双向过滤器：
```elisp
  (defun my-org/skip-repeated-todos ()
    "Skip TODO items that have the LAST_REPEAT property."
    (let ((repeated (org-entry-get nil "LAST_REPEAT")))
      (when repeated
        (progn
          (outline-next-heading)
          (if (eobp)  ; End of buffer check
              (point-max)  ; Move to the end of the buffer
            (point))))))
  (defun my-org/skip-non-repeated-todos ()
    "Skip TODO items that do not have the LAST_REPEAT property."
    (let ((repeated (org-entry-get nil "LAST_REPEAT")))
      (if (not repeated)
          (progn
            (outline-next-heading)
            (if (eobp)  ; End of buffer check
                (point-max)  ; Move to the end of the buffer
              (point)))  ; Continue to the next heading
        nil)))

```

然后再配合`org-super-agenda`，构成了完美的日期自动管理系统。
再然后就可以快意恩仇、歇斯底里、大杀四方！！！

